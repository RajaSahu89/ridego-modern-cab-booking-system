{
  "name": "ridego",
  "private": true,
  "engines": { "node": ">=18.18" },
  "scripts": {
    "setup": "node scripts/init-env.js",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && node scripts/init-env.js",
    "db:setup": "npm run db:setup --prefix backend",
    "dev": "concurrently -n api,web -c green,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
